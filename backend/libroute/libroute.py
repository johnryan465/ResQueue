from util import Point, Solution

def get_routes(start, people, vehicle_sizes):
    pass

# def euclidian(a, b):
#     return ((a[0] - b[0]) ** 2) + ((a[1] - b[1]) ** 2)
# 
# def shortest_route(start, points):
#     pass
# 
# def rescue_clusters(max_sizes, points, dist=euclidian):
#     pts = points[:]
#     clusters = []
#     for sz in max_sizes:
#         clust = []
#         if pts:
#             u = pts.pop()
#             sz -= 1
#             clust.append(u)
#             pts = sorted(pts, key=lambda a: dist(a, u), reverse=True)
#             while pts and sz > 0:
#                 clust.append(pts.pop())
#                 sz -= 1
#         clusters.append(clust)
#     return clusters

